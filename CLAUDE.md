## Design Context

`web/` (the Next.js frontend) has `PRODUCT.md` and `DESIGN.md` at its root, written by `/impeccable init`. Read both before any UI/design work in `web/` — they capture the register (product), brand personality ("Heraldic Ledger"), and the extracted color/typography/motion/component system already live in the code. Don't reinvent tokens or personality that are already documented there.
